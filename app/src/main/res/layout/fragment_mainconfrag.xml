<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/mainconcontainer"
    >

    <!--
    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclemain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingEnd="40dp"
        android:paddingBottom="50dp"
        android:clipToPadding="false"/>   -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclemain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingEnd="35dp"
        android:paddingBottom="50dp"
        android:clipToPadding="false"/>


<!---
        <cdflynn.android.library.scroller.BubbleScroller
            android:id="@+id/scroller"
            android:layout_width="100dp"
            android:layout_alignParentEnd="true"
            android:layout_height="match_parent"
            android:paddingEnd="@dimen/activity_horizontal_margin"
            android:paddingBottom="@dimen/activity_vertical_margin"
            android:paddingTop="@dimen/activity_vertical_margin"
            app:bubbleScroller_highlightColor="@color/lightblue"
            app:bubbleScroller_textColor="@android:color/white"
            app:bubbleScroller_textSize="@dimen/text_size_small"/>
            -->

    <com.gjiazhe.wavesidebar.WaveSideBar
        android:id="@+id/side_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingRight="8dp"
        app:sidebar_position="right"
        app:sidebar_max_offset="80dp"
        app:sidebar_lazy_respond="false"
        app:sidebar_text_color="#e9d2c2"
        app:sidebar_text_size="14sp"
        app:sidebar_text_alignment="center"
        android:layout_marginBottom="10dp"/>
    <LinearLayout
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:layout_width="fill_parent"
        android:layout_centerInParent="true">
        <ProgressBar
            android:id="@+id/progrss"
            android:minHeight="20dip"
            android:maxHeight="20dip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:visibility="gone"

            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/vazir"
            android:id="@+id/progtxt"
            android:layout_gravity="center"
            android:layout_marginTop="10dp"
            android:textColor="#fff"
            android:visibility="gone"/>
    </LinearLayout>


 </RelativeLayout>